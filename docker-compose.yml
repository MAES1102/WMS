services:
  app:
    build:
      context: .
    ports:
      - "8000:8000"
    environment:
      INVOICE_DATABASE_URL: sqlite:////data/invoice.db
      INVOICE_STORAGE_ROOT: /data/documents
    volumes:
      - invoice_data:/data
    restart: unless-stopped

volumes:
  invoice_data:
