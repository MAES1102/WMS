services:
  app:
    build:
      context: .
    ports:
      - "8000:8000"
    environment:
      WORKFLOW_DATABASE_URL: sqlite:////data/workflow.db
    volumes:
      - workflow_data:/data
    restart: unless-stopped

volumes:
  workflow_data:
